package ConditionalsPractice;

public class Assignment1 {
    public static void main( String[] args ){

        // 1: The elseif statements act like "backup" plans when the if statement fails. Meanwhile, the else option acts like a last resort option if the rest of the ifs/elseifs are false.
        // 2: The difference is that removing the else no longer results in a chain of if statements the prior if statement fails. Instead, it acts like a regular if statement and occurs by itself regardless of whether the previous if statement executed.//
    }
}
